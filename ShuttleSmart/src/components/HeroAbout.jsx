import React from 'react';

const HeroAbout = () => {
  return (
    <section className="hero-about">
      {/* Content for this hero seems minimal, mainly a large image */}
    </section>
  );
};

export default HeroAbout;